[project]
name = "pywifi"
version = "1.1.12"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "comtypes>=1.4.11",
]

[dependency-groups]
dev = [
    "pytest>=8.4.0",
]
